## Todo
- Get tooltips working (fork tooltips repo)
- Have a way to exclude whole headers without calling them Secret
- Remove files that no longer exist in the vault
- Rewrite the index file to explain the current main conflict
